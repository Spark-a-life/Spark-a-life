import {promises as fs} from "node:fs";import path from "node:path";const out=path.resolve("dist");await fs.rm(out,{recursive:true,force:true});await fs.mkdir(out,{recursive:true});for(const name of ["src","configs","schemas","prompts","docs","examples","README.md","LICENSE","package.json"]){await fs.cp(path.resolve(name),path.join(out,name),{recursive:true})}console.log(`Built distributable at ${out}`);
