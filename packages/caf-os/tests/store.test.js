import test from "node:test";import assert from "node:assert/strict";import {mkdtemp} from "node:fs/promises";import {tmpdir} from "node:os";import path from "node:path";import {ProjectStore} from "../src/core/store.js";
test("witness records are hash linked",async()=>{const s=new ProjectStore(await mkdtemp(path.join(tmpdir(),"caf-")));await s.init({id:"test-project",title:"Test",purpose:"Test",audience:"Test",createdAt:new Date().toISOString()});await s.appendWitness("test-project","test.event",{ok:true});const rows=await s.readWitness("test-project");assert.equal(rows.length,2);assert.equal(rows[1].prevHash,rows[0].hash)});
