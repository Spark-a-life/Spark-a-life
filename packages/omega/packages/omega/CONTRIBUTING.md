# Contributing

Changes must include tests, preserve fail-closed behaviour, avoid hidden external calls, update schemas and documentation when contracts change, and pass `pytest -q` plus `ruff check src tests`.
