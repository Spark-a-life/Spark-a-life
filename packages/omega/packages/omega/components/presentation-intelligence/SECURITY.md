# Security

Never store credentials in project files. Treat generated artefacts as untrusted until review gates pass. External adapters must use environment variables and explicit network configuration.
