# Threat Model

Principal threats include prompt injection, evidence poisoning, false consensus, authority bypass, credential leakage, runaway loops, audit tampering and unsafe external egress.

Controls include schema validation, context isolation, scoped contracts, default-deny external actions, Captain's Gate, deterministic budgets, hash chaining and mandatory verification. Residual risk must be accepted by accountable deployment owners.
